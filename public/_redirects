# 健康检查端点（放在最前面以提高性能）
/health    /health.json   200
/status    /build-info.json   200

# 静态资源重定向
/assets/*    /assets/:splat    200

# SPA 路由重定向 - 所有未匹配的路由都重定向到 index.html
# 注意：避免与具体路径冲突，使用更精确的匹配
/notes/:slug    /index.html   200
/admin/:path    /index.html   200

# 通用 SPA 回退（放在最后）
/*    /index.html   200
